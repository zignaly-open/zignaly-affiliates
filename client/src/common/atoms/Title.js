import styled from 'styled-components';

const Title = styled.div`
  font-size: 1rem;
  font-weight: bold;
  margin-bottom: 15px;
  margin-top: 20px;
  &:first-child {
    margin-top: 0px;
  }
`;

export default Title;
